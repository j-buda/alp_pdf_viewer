name: alp_pdf_viewer
description: A new flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.alpian.alp_pdf_viewer
        pluginClass: AlpPdfViewerPlugin
      ios:
        pluginClass: AlpPdfViewerPlugin